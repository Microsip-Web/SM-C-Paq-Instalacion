${
pack.updated
? `<p class="pack-updated">Actualizado el ${pack.updated}</p>`
: ''
}
${pack.size ? `<p class="pack-size">Tamaño: ${pack.size}</p>` : ''}

${
pack.updated
? `<p class="pack-updated">Actualizado el ${pack.updated}</p>`
: ''
}
${pack.size ? `<p class="pack-size">Tamaño: ${pack.size}</p>` : ''}
